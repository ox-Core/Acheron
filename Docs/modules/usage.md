# Usage

The modules in this section can be used by calling ``world.ImportModule<ModuleName>()``, these modules will likely create singletons or behaviour, these will be specified in each modules section.